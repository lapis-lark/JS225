/*
  methods = functions with a receiver
    a receiver is the object a method is called on

  no explicit receiver = function invocation
  explicit receiver = method invocation

  keyword `this` references the receiver during method invocation

  collaborators = objects storing states within other objects




*/